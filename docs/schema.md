# Schema summary

Overview
- High-level list of schemas and important tables.
- For full DDL see `docs/schema-ddl.sql` (generated by `pg_dump -s`).

ER diagram
- Export an ERD (DBeaver / pgAdmin) as `docs/erd.png` and reference it here.

Quick commands
- Generate DDL: `pg_dump -h 127.0.0.1 -p 54322 -U postgres -s --no-owner --no-privileges -f docs/schema-ddl.sql`
