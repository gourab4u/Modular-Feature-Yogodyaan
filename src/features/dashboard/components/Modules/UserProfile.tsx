import React from 'react';

const UserProfile: React.FC = () => {
    return (
        <div>
            <h1>User Profile</h1>
            <p>This is the user profile module.</p>
        </div>
    );
};

export default UserProfile;